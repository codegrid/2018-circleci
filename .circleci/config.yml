version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run: npm run build
      - run: zip -r dist.zip dist
      - store_artifacts:
          path: dist.zip
